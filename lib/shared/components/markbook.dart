import 'package:flutter/material.dart';

class MarkBook extends StatelessWidget {
  const MarkBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/bookmark.png'),
    );
  }
}
