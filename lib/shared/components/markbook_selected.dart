import 'package:flutter/material.dart';

class MarkBookSelected extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/bookmarkSelected.png'),
    );
  }
}
